# TODO

## 会話履歴のコンテキスト管理

現行MVPは、WebSocket接続ごとに直近20メッセージをメモリ保持する。通常は約10往復分だが、件数だけで制限しているため、長い発話や応答が続くとLLMのコンテキスト上限を圧迫する可能性がある。

- [ ] LLMへ送る前に、システムプロンプト・会話履歴・今回の発話を含む入力トークン数を概算または利用モデルのトークナイザーで計測する。
- [ ] `MAX_HISTORY_MESSAGES`に加えて、モデルごとに設定可能な入力トークン予算を導入する。
- [ ] 予算超過時は、古い履歴をメッセージ単位ではなく「ユーザー発話＋AI回答」の往復単位で削除する。
- [ ] 削除対象の履歴を短い日本語要約へ置き換える会話要約機能を追加する。要約失敗時は、古い往復を安全に削除して会話を継続する。
- [ ] STT結果に文字数上限を設け、上限超過時の扱い（切り詰め、再発話案内、エラー）を定義する。
- [ ] トークン予算超過、要約成功・失敗、削除件数を会話本文を含めずにログ・メトリクスへ記録する。

### 受け入れ条件

- 長時間の会話でも、設定した入力トークン予算を超える履歴をLLMへ送らない。
- 履歴削減後も直近の会話と要約された文脈を利用して応答できる。
- 会話リセット、切断、キャンセル時の現在のメモリ解放・非永続化の挙動を維持する。
